apiVersion: v1
kind: Service
metadata:
  name: microservice-demo
  labels:
    app: microservice-demo
spec:
  type: NodePort
  selector:
    app: microservice-demo
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080
